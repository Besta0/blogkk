# 更新日志 / Changelog

## [2.0.1] - 2026-02-02

### 📸 新增 / Added

#### 截图展示 / Screenshots
- ✅ 添加5张应用截图到 README
- ✅ 首页、关于、项目、博客、联系页面完整展示
- ✅ 中英文 README 同步更新

### 🔧 配置优化 / Configuration

#### .gitignore 更新
- ✅ 排除 `.kiro/` AI 助手文件
- ✅ 排除本地脚本和说明文件
- ✅ 确保敏感文件不会被上传

### 📚 文档完善 / Documentation
- ✅ 添加截图展示部分到 README
- ✅ 优化文档结构和可读性

---

## [1.1.0] - 2026-02-01

### 🐛 Bug 修复 / Bug Fixes

#### 博客文章编辑"无效ID"错误 / Blog Post Edit "Invalid ID" Error
**问题描述**: 在管理后台编辑博客文章时显示"无效ID"错误

**根本原因**:
1. 后端控制器返回 `_id` 字段，但前端 TypeScript 类型期望 `id` 字段
2. 前端有复杂的 ID 转换逻辑尝试处理数字和字符串 ID
3. 后端删除方法有逻辑错误 (`if (!!id)` 应该是 `if (!id)`)
4. 端口配置不一致

**修复内容**:
- ✅ 修改后端控制器所有响应从 `_id` 改为 `id`（7处）
- ✅ 简化前端 BlogManager 的 ID 处理逻辑
- ✅ 修复后端 `deletePost` 方法的逻辑错误
- ✅ 统一开发环境端口配置（前端 5050，后端 3010）
- ✅ 统一生产环境端口配置（前端 5060，后端 3020）

**影响的文件**:
- `backend/src/controllers/blog.controller.ts`
- `src/admin/pages/BlogManager.tsx`
- `docker-compose.dev.yml`
- `docker-compose.yml`
- `backend/.env`
- `backend/.env.production.example`
- `.env.production.example`

### ⚙️ 配置更新 / Configuration Updates

#### 端口标准化 / Port Standardization
**开发环境**:
- 前端: 5050
- 后端: 3010
- MySQL: 3308 (主机) / 3306 (容器)
- MongoDB: 27019 (主机) / 27017 (容器)

**生产环境**:
- 前端: 5060
- 后端: 3020
- MySQL: 3307 (主机) / 3306 (容器)
- MongoDB: 27018 (主机) / 27017 (容器)

#### JWT 令牌过期时间 / JWT Token Expiration
- 从 15 分钟延长到 1 小时
- 改善用户体验，减少频繁重新登录

### 📚 文档更新 / Documentation Updates

**新增文档**:
- `PRODUCTION_CHECKLIST.md` - 生产环境部署检查清单
- `PRODUCTION_QUICKSTART.md` - 5分钟快速部署指南
- `CHANGELOG.md` - 更新日志（本文件）

**新增脚本**:
- `prod-test.sh` - 生产环境自动测试脚本
- `verify-fixes.sh` - 修复验证脚本

**更新文档**:
- `README.md` - 已包含完整的开发和生产环境说明
- `README-zh.md` - 中文版同步更新

### 🔧 技术改进 / Technical Improvements

1. **ID 字段统一**: 所有 API 响应使用 `id` 而不是 `_id`
2. **UUID 验证**: 使用专门的 `validateUUID` 中间件
3. **端口映射**: 修复 Docker 端口映射配置
4. **环境变量**: 统一开发和生产环境配置

### ✅ 验证 / Verification

运行验证脚本确认所有修复：
```bash
./verify-fixes.sh
```

所有 19 项检查通过 ✅

---

## [1.0.0] - 2026-01-30

### 🎉 初始版本 / Initial Release

**功能特性**:
- ✅ 动态个人作品集网站
- ✅ 博客系统（Markdown 支持）
- ✅ 项目展示
- ✅ 联系表单
- ✅ 管理后台
- ✅ 分析统计
- ✅ 图片上传（Cloudinary）
- ✅ 邮件通知（SMTP）
- ✅ MySQL + MongoDB 混合数据库
- ✅ Docker 容器化部署
- ✅ JWT 认证
- ✅ 响应式设计

**技术栈**:
- Frontend: React + TypeScript + Vite + TailwindCSS
- Backend: Node.js + Express + TypeScript
- Database: MySQL (结构化数据) + MongoDB (分析数据)
- Deployment: Docker + Docker Compose

---

## 升级指南 / Upgrade Guide

### 从 1.0.0 升级到 1.1.0

#### 开发环境

1. **停止当前服务**:
```bash
docker-compose -f docker-compose.dev.yml down
```

2. **拉取最新代码**:
```bash
git pull origin main
```

3. **更新配置文件**:
```bash
# 检查 backend/.env 中的 PORT
# 应该是 PORT=3010
```

4. **重新构建并启动**:
```bash
docker-compose -f docker-compose.dev.yml up -d --build
```

5. **验证**:
```bash
curl http://localhost:3010/api/health
curl http://localhost:5050
```

#### 生产环境

1. **备份数据**:
```bash
# MySQL 备份
docker exec portfolio-mysql mysqldump -u root -p portfolio > backup_$(date +%Y%m%d).sql

# MongoDB 备份
docker exec portfolio-mongodb mongodump --out=/backup
```

2. **停止服务**:
```bash
docker-compose down
```

3. **拉取最新代码**:
```bash
git pull origin main
```

4. **更新配置**:
```bash
# 检查 backend/.env.production 中的 PORT
# 应该是 PORT=3020

# 检查 JWT_EXPIRES_IN
# 建议设置为 1h
```

5. **重新构建并启动**:
```bash
docker-compose up -d --build
```

6. **验证部署**:
```bash
./prod-test.sh
```

### 破坏性变更 / Breaking Changes

**无破坏性变更** - 此版本完全向后兼容 1.0.0

所有更改都是内部实现的改进，不影响 API 接口或数据结构。

---

## 已知问题 / Known Issues

目前没有已知问题。

如果发现问题，请在 GitHub Issues 中报告。

---

## 贡献者 / Contributors

感谢所有为此项目做出贡献的开发者！

---

**注意**: 此更新日志遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 规范。
